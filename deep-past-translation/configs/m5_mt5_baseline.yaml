# Model 5: mT5-small baseline
model:
  name: google/mt5-small
  max_length: 512

training:
  epochs: 20
  learning_rate: 1.0e-4
  batch_size: 2
  gradient_accumulation: 4
  label_smoothing: 0.2

data:
  use_sentence_align: true
  use_bidirectional: false

output:
  dir: ./models/m5_mt5_baseline
